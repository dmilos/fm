
namespace fileM
 {
  namespace view
   {
    namespace panel
     {

      class selection
       {
        public:

         selection();
        ~selection();

         //void   list( unsigned group, FM_ListPtr *list );
         //bool   attribs( unsigned group, FM_Attribs const &attribs );
         //bool   unselect( unsigned group );
         //void   unselect_all( void );

      };

     }
   }
 }