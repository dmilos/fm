#include "reflection/object.hpp"

namespace fileM
 {
  namespace view
   {
    namespace panel
     {

     class cursor
      {
       public:

        cursor();
       ~cursor();

        //bool       up( void );
        //bool       down( void );
        //size_t     position();
        //size_t     remove(size_t begin, size_t end );
     };

     }
   }
 }